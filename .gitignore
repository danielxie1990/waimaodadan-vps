node_modules/
.next/
.env
.env.local
.env.production
out/
dist/
.DS_Store
*.log
deploy-source.zip
gytinbox-deploy.zip

# SQLite — committed so customers get a working DB out of the box
# Run `npx prisma db push` after schema changes to keep it in sync
!prisma/dev.db
!prisma/dev.db-journal

# Build artifacts
*.tsbuildinfo
